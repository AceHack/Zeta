# Projection implementation archive: independent admission

Date: 2026-09-08 UTC
Operational status: research-grade independent archive review
Reviewer: Vera, OpenAI Codex using GPT-6 Astra
Work item: 081M1Z63YMC087G0R003N5FH9X

Accepted within the declared source/direct-file scope at publication
`ba7c312d55233f507ecbbfd62e3c3fb733fd35a7`, source commit
`f33ac42959388344fc3c82058f165764954fa22d`. The independently supplied manifest
is exactly 9821 bytes, SHA256
`FE1F5BDF732FA6B08092887210552BA9CEF68D724E519C77958A282366A348E7`.
This closes the bounded archive-review prerequisite. It neither executes nor
admits a numerical result, actual driver invocation or complete runtime closure.
A verified remote source ref remains a separate execution prerequisite.

## Implementation identities and reservation

The [retained independent audit](projection-implementation-archive-review/2026-09-08/README.md)
checks exact stored hashes, complete single-member gzip framing, and all regular
tar members without extraction. The implementation archive has 59 members,
11818524 payload bytes and 5152737 stored bytes. Its stored SHA256 is
`56B80DBAC2D4D8430D4B57D2F429529582457D34582FD9D94E62EE82578502A1`.

All 47 ordered source rows equal the reviewed driver's fixed roster. Every
length/hash and source body matches the committed source, publication commit
and original current file. Individual/aggregate limits and the exact registered
protocol hash agree. All five native roles, in order, match the manifest and
custody mapping: host, replay script, Core DLL, Core.Abstractions DLL, Bayesian
DLL. Their original and archived bytes match; the replay script is shared with
its source row. These are 51 unique original source/native files, not 52
independent files. The freeze helper and duplicated manifest/custody records
also match. Environment outputs are retained capture bytes; no separate
original environment-output files are invented.

The three DLLs are retained build-output files observed after the recorded
full gate. This is direct file custody, not independent proof that source maps
to those machine instructions or that all transitive loads are pinned. Python
installed-distribution metadata likewise does not authenticate every wheel file.
The freeze helper was read as data; it performs source/file observations,
installed-distribution inspection and dotnet info, with no numerical API or
driver invocation. Its outer exit-zero observation expressly lacks a separately
captured original invocation timestamp; none is reconstructed here.

I independently recomputed the approved reservation from 10502579 copied
script/DLL bytes, 9821 manifest bytes and the fixed native/metadata allowances:
149737504 driver plus 118697952 inner combined bytes equals 268435456 bytes;
90 plus 422 artifact slots equals 512. The inner share includes its 8388608-byte
journal and runner terminal allowance. No refund is made. External coordinator
argv/exit/time/stdout/stderr records may remain explicitly outside this attempt
budget; they must not be described as covered driver files or a full-result dump.
These are retention limits, not OS disk/heap or total factory-cost limits.

## Assembled validation

The second archive has 89 members, 1344436 payload bytes and 226790 stored bytes;
SHA256 `B84F38656894FAA0EF03F8CA7497DBAE5ABC10292B88CC7E8A7C5E3F24B317B6`.
Every member matches its manifest and local original.

The full gate at `096b09126b1f489fc7720b69780263ef01485445` reports all 18 checks
passed, exit zero, and unchanged clean head/source observations for all 47 rows.
Formatter exit zero retains workspace warnings and explicit unsupported F#
projects; this does not establish F# formatting verification.

The first separately captured 14-file Python snapshot equals that gate's source
and has 340 tests passing in 7.53 seconds, configured mypy/format clean, and one
root-cwd Ruff import-separator refusal. The second snapshot has 340 passing in
7.07 seconds and all four checks clean. Its recorded **base head** is
`fdc2e42e0ffdb9f5b295ab0f8c31a05b8a730905`; one test line was already changed
in the executed snapshot. All fourteen executed files match final source f33.
The actual mypy invocation uses `--follow-imports=silent` with the repository's
configuration; this review does not invent a different command.

Across all 47 source paths, only that one blank line in the driver test differs
between full-gate and frozen source. Production, numerical contract and build
wiring are unchanged. The assembled record correctly preserves main's graph
and accepted kernel ADR as the two initial differences from coordinator source,
the original command-not-found install diagnostic, and the later successful
explicit install. No earlier check is reassigned to edited bytes.

My first audit incorrectly required the second Python snapshot to equal its
recorded base head. It stopped at the documented one-byte test difference.
The exact failed script/stdout/stderr and disposition remain retained; the
corrected audit checks that difference explicitly and binds the snapshot to
final source. This was a reviewer assertion correction, not an archive defect.
The completed audit exited zero with empty stderr. No preparation, driver,
solver, native numeric process or final 40-case/88-call workload was run.

Signed: Vera, OpenAI Codex using GPT-6 Astra.
