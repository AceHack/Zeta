# Guarded hidden-switch compilation: actual strict-epsilon witness

Date: 2026-09-07
Operational status: research-grade
Lifecycle: active
Work item: 081M1XXWTTF087G0R000X1HMD0
Author: Vera, OpenAI Codex using GPT-6 Astra
Artifact status: untimed supplied-Q conformance; complete outer/runtime admission pending

The actual unchanged native selector returned harvest at an exact epsilon tie.
An explicitly executed inclusive `>=` mutant returned switch. The independent
software binary64 replay accepted the strict control and rejected the mutant
through the same action-checking boundary. This is an executable selector
witness, separate from the earlier certificate-declaration mutation.

The supplied Q encodings were `0000000000000000` and `3D719799812DEA11`.
The independently evaluated subtraction was exactly `3D719799812DEA11`, equal
to the admitted certificate epsilon. Actual native action was zero; mutated
action was one; the independent mutation refusal was `SelectorAction`.
No source tape or measured policy arm was introduced by this supplied-Q call.

Native witness source is `8aede9982fc3ce1eb891c5d630710f778b3eff63`.
Its incomplete invocation envelope is 7,457 bytes with SHA256
`9EBBC621583A24BF45DB3AD50F665CCD3EF3D4A7070004F9CDF4E58D705FD78D`.
The independent [pure checker](../../src/Interp.Python/zeta_interp/hidden_switch_compiled_outer_negatives.py)
and tests are pinned at `16d2ac85bd00ce6664731470a3c5f8345f9df4fb`.
The [lossless inventory](hidden-switch-compiled-validation/2026-09-07/selector-tie-replay-attempt-1/manifest.json)
retains the exact native input, first replay harness/result/stdout/stderr,
source/test identities and validation logs. Raw native input was unchanged
before and after replay. Six observed loaded task-source files matched the
independent commit's blobs; this finite observation is not full Python entry,
module closure, source-to-bytecode or runtime admission.

The pure checker requires an independently issued certificate and the exact
nested witness schema. Its positive control invokes the independent selector
on the retained Q bits. The mutated output must differ with the correct exact
integer type and fail the same action boundary. Injected inclusive-reference
and permissive-action-checker tests ensure these checks cannot silently become
vacuous. Booleans, float aliases, altered bit rosters and extra keys refuse.

The first suite passed 23 cases in 3.85 seconds while strict mypy rejected a
list-variance annotation. After an explicit JSON-list annotation and unchanged
string-tuple result construction, the final suite passed **23 cases in 3.71
seconds**; strict source/test mypy, Ruff and format checks passed. Independent
read-only source review accepted that final pin before actual native replay.
The first actual replay then exited zero. Earlier logs remain preserved.

This replay checks only `SelectorWitness`. It does not admit the surrounding
ten invocation rows, a whole hand/negative envelope, executing native body,
source archive or runtime. The separate [actual native hand replay](2026-09-07-hidden-switch-compiled-native-slice-replay.md)
retains its independently established 222/48/24 slice scope. No registered
behavior/cost stream has been generated and no speed claim follows.

## Separate historical inventory audit

The inventory audit at integration tree `4f2f70d6e` checked stored/original
lengths and hashes and exact single-member gzip relations for **3,872 distinct
artifacts**, with zero remaining verification failures. This is byte preservation
evidence, not scientific or executing-code admission.

The first audit incorrectly applied the final six-field phase descriptor's
`File` interpretation to historical seven-field capture inventories. In those
inventories `File` retains the original provenance path and `StoredFile` names
the actual preserved file. That audit verified 1,294 records and reported 2,578
missing paths under the wrong interpretation. The corrected audit explicitly
projects `StoredFile` for those historical records, then invokes the real strict
byte-binding checker. The original failure report, complete corrected report
and corrected audit source are retained losslessly. No historical manifest or
artifact was rewritten, and the final phase's exact six-field schema is unchanged.
