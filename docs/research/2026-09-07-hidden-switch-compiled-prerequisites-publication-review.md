# Guarded controller: prerequisite publication review

Date: 2026-09-07
Operational status: research-grade
Lifecycle: active
Publication work item: 081M1YYRTYF087G0R003TXBK2W
Parent experiment: 081M1XXWTTF087G0R000X1HMD0
Reviewer: Vera, OpenAI Codex using GPT-6 Astra
Disposition: source/evidence scope accepted; final gates and release remain pending

This bounded read-only review covers publication head
`2d1aff7686823c050fad8e093ac89a3180f29b48` in the dedicated publication
writer, based on main `fe90e36e80be59da23cc6c382b8bf377a75349f9` and
source cutoff `f6830f83cbafd6b1a992a8a9bdf6bb1db87dd3fa`.
It checks publication integration and inherits the previously recorded exact
source acceptances; it is not a repeat of every mathematical or source review.
I executed no build, test, fixture, policy, native process, analyzer, source
stream or measurement. The publisher's full preflight was still running at
this review boundary; no green gate or main-publication claim follows here.

The [retained snapshots](hidden-switch-compiled-validation/2026-09-07/prerequisites-publication-review/manifest.json)
include every one of the 2,298 changed paths. There are 68 paths under `src/`
and five under `tests/`; the compiled validation subtree contributes 2,069
paths. Including original-study and merge-observer evidence gives 2,170 paths
across the three evidence roots. These are distinct counting categories, not
an incomplete first page. No raw `.core` or `.dmp` path, including gzip forms,
appears in the changed inventory.

All reviewed source/wiring blobs match the cutoff except
`tests/Tests.FSharp/Tests.FSharp.fsproj`. Its sole difference from that cutoff
is the `ZetaFsFuseAbi.Tests.fs` link inherited from the main integration; the
new compiled test links remain intact. The solution's file-backed CLI wiring
and previously reviewed two-row mutation-mediated check-arity census are
unchanged from the cutoff. Across the three evidence roots, the sole blob
difference is the new publication link in the compiled validation index.
Historical failures and raw receipts were not replaced by this integration.

I independently hashed every one of the 83 integration-five Interp source/config
pins from the publication git tree; all match the retained 983-test source
manifest. That establishes source correspondence to the reported subset,
not a full-suite count or transfer of the later publication gate. All nineteen
original scientific files and eighteen original result records match their
retained main-proof lengths and SHA-256 values. The frozen protocol still hashes
to `8bbdfe44a0844dd8ce4f6c5dd77b060a56e5b84ea94ea7a6fdbb482aec9d738a`.
This pass does not freshly query or move remote archive refs.

The new ClrMD helper/project/driver and native-dependent fixture module are absent
from this source slice. Their continuing work cannot enlarge the publication's
accepted scope. The CLI dispatch exposes certificate checks, incomplete hand
slices and graph diagnostics; behavior/cost execution still refuses. The
publication note, handoff and validation index distinguish actual native hand
replay and Python subset checks from full outer/runtime admission. They retain
placeholder binding limits, the three historical SOS refusals, the overwritten
old-DLL custody limitation, first failed gates and advisory publication reds.
No speed, registered experiment result or immutable implementation archive is
claimed.

I checked 502 relative local link targets across all 66 changed Markdown files;
none is missing. This checks target existence outside fenced code, not HTTP
availability or heading anchors. I also read the publication note, both claim
files, finite work item and updated handoff. The finite publication and parent
implementation are kept distinct. At this exact reviewed head both claim
files remain present: their later publication-tree removal and finite task
completion are still required. Removing these tree entries must not release
or erase the active parent claim on its separate remote branch.

No material publication source/evidence finding remains. Acceptance is limited
to this exact pre-release head and the stated inherited source boundaries.
The publisher must still preserve the current gate outcome, make the ordinary
publication-only claim/task update, and verify the resulting final path
inventory, required checks, review state, full signed squash body and main
ancestry. New source edits would require renewed source correspondence review.
Complete outer conformance, actual runtime/body/call closure and the separate
reviewed implementation archive remain prerequisites to registered 9307/9409
source generation and measurement.

```text
Agency-Signature-Version: 1
Agent: Vera
Agent-Runtime: OpenAI Codex
Agent-Model: GPT-6 Astra
Credential-Identity: AceHack
Credential-Mode: shared
Human-Review: not-implied-by-credential
Human-Review-Evidence: none
Action-Mode: autonomous-fail-open
Task: 081M1YYRTYF087G0R003TXBK2W
Co-Authored-By: Codex <noreply@openai.com>
```
