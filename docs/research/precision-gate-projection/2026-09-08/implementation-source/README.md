# Frozen implementation source for the registered projection comparison

Date: 2026-09-08 UTC
Author: Vera, OpenAI Codex using GPT-6 Astra
Operational status: research-grade
Status: source frozen; independent archive admission pending; final workload unopened

The independently prepared [input manifest](implementation-manifest.json) has
SHA256 `FE1F5BDF732FA6B08092887210552BA9CEF68D724E519C77958A282366A348E7`.
Its [digest file](implementation-manifest.sha256) is a convenience copy; the
actual invocation must supply the independently retained digest explicitly.
It is not an expectation derived from a numerical producer or its receipt.

The [archive manifest](archive-manifest.json) binds all 59 members of the
[custody archive](custody.tar.gz), verified by decoding without extraction.
It contains all 47 source paths, the exact input manifest, the already-built
three direct DLLs and actual host bytes, source custody metadata, environment
observations and the source-freezing helper. The replay script is present once
under sources and serves both its source and native-role identities.

Every source byte equals committed source at
f33ac42959388344fc3c82058f165764954fa22d. Its merge with main retained all
47 source identities. Compared with the complete gate at
096b09126b1f489fc7720b69780263ef01485445, only one blank line in the driver
test changed; production source, numerical contract and build wiring are
unchanged. Both complete 340-test source cuts and the final strict/lint results
are retained in [assembled validation](../assembled-validation/README.md).
The original native binaries were produced by that full gate and are archived
as observed; this is not a source-to-machine proof or transitive runtime closure.

The [custody metadata](source-custody.json) maps each native role to its original
and archived path. Python output records installed distribution metadata, not
an assertion that all wheel contents were bound. Dotnet info is an actual
observation. Later driver snapshots must still admit the actual loaded local
file/origin paths and before/producer/after direct-file identities.

The driver reservation is exactly 149,737,504 combined raw-plus-stored bytes
and 90 slots. The recorder receives 118,697,952 combined bytes and 422 slots,
including its 8,388,608-byte journal and separate 2-MiB combined terminal reserve.
These sum to the registered 256-MiB/512-slot ceilings. Copied script/DLL bytes
are 10,502,579 and manifest bytes are 9,821. Unused reservations are not reclaimed.
This is the declared retention scope, not an OS disk/heap quota or all factory,
build and archival cost. Existing committed source archives are not copied into
the final run directory again.

The [freeze observation](freeze-observation.json) and original stdout/stderr
retain the actual tool-observed exit and its timestamp limitation. Freezing ran
only source/file checks, installed-distribution inspection and dotnet info.
No NativeSolve, ReferenceRoot, CertifyNative or final driver run occurred.
Complete independent archive review and a verified remote source ref precede
opening the fixed 40-ID/88-call workload.
