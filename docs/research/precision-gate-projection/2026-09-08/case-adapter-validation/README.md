# Scalar projection case adapter: first source validation

Date: 2026-09-08 UTC
Operational status: research-grade implementation evidence
Author: Vera, OpenAI Codex using GPT-6 Astra
Work item: 081M1Z63YMC087G0R003N5FH9X

The [case adapter](../../../../../src/Interp.Python/zeta_interp/precision_gate_projection_cases.py)
implements the registered finite roster, exact string rendering, duplicate-key
wire control and fresh single-mutation transformations. It executes no solver,
reference, process or final comparison. The mutation helper cannot establish
actual baseline provenance; the future run coordinator must retain an actual
certified core/unconstructed result before invoking certificate controls.

The first source check found one import-order defect. Its attempted pytest
launch used the global interpreter, which has no pytest, and failed before
tests ran. The original diagnostics remain in the [manifest](manifest.json).
After the import/format correction, the existing project environment ran
31 dedicated tests successfully in 5.65 seconds. Ruff and focused mypy passed.
No dependency installation or numerical solver execution was used to repair
that launch. The first pre-format file bytes were not separately snapshotted;
the manifest identifies source at the passing attempt, not invented first-cut
source custody.

Tests distinguish exact stationary centers, signed-zero/string retention,
duplicate keys, nonfinite and malformed candidate input, mutation isolation
and exact nearest-even rounding when +1 is absorbed at a binary64 midpoint.
Disposable candidate fixtures are explicitly unit fixtures, never actual
certified evidence. These are adapter tests, not the registered 40-case,
88-call numerical experiment or evidence that its viability criteria pass.

The [registration](../../../2026-09-08-precision-gate-projection-registration.md)
and [decimal clarification](../../../2026-09-08-precision-gate-projection-decimal-admission-clarification.md)
remain unchanged. Independent implementation review, source archival and
complete numerical process custody remain required before final comparison.
