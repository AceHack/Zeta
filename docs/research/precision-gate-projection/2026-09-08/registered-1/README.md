# Registered scalar projection comparison: first actual attempt

Date: 2026-09-08 UTC
Author: Vera, OpenAI Codex using GPT-6 Astra
Operational status: research-grade experimental record
Status: actual fixed workload completed; independent outcome review pending

The sole `registered-1` invocation completed with exit zero in 40.661787667
seconds. All 88 registered calls across 40 subject IDs returned retained,
checked receipts. All twelve required core candidates were independently
certified. Twenty-seven native children started and closed. There are no pending
calls, raised service calls, primary failure or secondary failures.

This is a finite scalar Gaussian-family projection result. It is not a learned
model, global inference proof, held-out score or state-of-the-art comparison.
The [result and investment disposition](../../../2026-09-08-precision-gate-projection-registered-results.md)
reports the numerical refusals and next integration boundary.

## Exact custody

The [manifest](manifest.json) binds all 336 regular members of the
[custody archive](custody.tar.gz): every one of the 328 actual attempt files,
five original external coordinator records, the actual execution helper,
the post-run archive helper and its derived observation. The archive is
6,397,793 bytes with SHA256
`04C079901CEA886500CFD0A5912CD812A997268803FAE3F0DAD4F8EDE65A8167`.
All member hashes and complete single-member gzip framing were checked without
extraction. Original local paths remain recorded for independent comparison.
The [readable observation](observation.json) is the same derived summary included
in the archive, not a replacement for native, reference or process receipts.

The frozen manifest hash supplied literally to the CLI was
`FE1F5BDF732FA6B08092887210552BA9CEF68D724E519C77958A282366A348E7`.
Source publication `ba7c312d55233f507ecbbfd62e3c3fb733fd35a7` and independent
archive admission `13ede4d8cfa7b69e579fe67ab477faefa5c07dab` were both verified
on their remote branches immediately before invocation. Executed checkout
`4937d7468` adds only that independent review to the source publication. All
47 source rows and five direct native roles matched before execution; all
47 source rows still matched after execution. The two actual driver source
snapshots have identical bytes. Exact CLI arguments, selected Python import
root, start/end timestamps, exit status and original stdout/stderr are retained.

The 328 attempt files occupy 21,527,800 bytes of file contents. This is a
post-run file census, not a measurement of total RAM, filesystem allocation,
CPU cost or the driver's charged retention sum. All 242 distinct artifact
paths referenced by the outer envelope, terminal and journal passed their
stored/raw byte and hash checks. The copied native artifacts and per-process
files are additionally present in the complete inventory.

The registered reservation remains 149,737,504 driver plus 118,697,952 recorder
combined raw/stored bytes and 90 plus 422 slots. The journal records a
pre-final-encoding reservation of 14,382,753 raw bytes, the same stored bytes
and 238 slots; it includes its reserved journal allowance and is not an actual
file-size total. No unused reservation is reclaimed. External coordinator
provenance and this later research archive are outside attempt retention scope.
No full in-memory DriverResult or RunResult dump was added to the attempt.

Direct-file observations do not prove transitive runtime closure or a mapping
from source to machine code. The 40.662-second single run includes Python
startup, repeated native startup and recording; it is not a comparative speed
benchmark. No numerical operation was rerun to construct this archive.
