# Independent projection root checkpoint and certificate findings

Date: 2026-09-08 UTC
Operational status: research-grade
Status: root component accepted; certificate correction required
Reviewer: Vera, OpenAI Codex using GPT-6 Astra
Work item: 081M1Z63YMC087G0R003N5FH9X

I accept the independently read root component and its four corrections at
51a96fa11bd7590af3d5ed45b6fb6ebfdca414fe. This does not accept the new
certificate consumer in the same module. Two later certificate findings are
recorded below. The [custody audit](projection-root-checkpoint-review/2026-09-08/README.md)
reads immutable source, ASTs and retained records; it performs no numerical
work, fixture run, native query or final evaluation.

The original root checkpoint is a04bba14c487f7dc72e5f7e01abf93cd471979d1.
Its module is 24,690 bytes, SHA256
3f30a828a4baca1a8fb5cea32f91946d8fc7b7bae959c72132fab4344a5ef651.
The combined 51a96 module is 52,146 bytes, SHA256
af42bc84b01a1ada681def42d5ac6499f0470fe7532c76782d052bde7e8057e7.
Its tests are 35,897 bytes, SHA256
3c82dfd97576771f26497554485504c9c386eac3bbd363f13f5aba985ccc7f21.
Both final source/test bytes equal the retained fifth combined attempt.

The root composes the separately [reviewed interval core](2026-09-08-projection-interval-core-independent-review.md)
with the [frozen mathematical contract](2026-09-08-projection-contract-independent-review.md).
It independently renders exact dyadic targets, retains nominal conversion
drift and checks caller bindings. Input type/length precede hashing. Domain
refusal retains a rendered target and numeric Starts before parameter work.
Every precision prepares its parameters again from original dyadics; only
actually admitted contexts appear in Contexts.

The initial outward bracket and both endpoint signs are admitted before use.
The monotonic phi derivative bound justifies the outward residual cone;
intersections preserve prior proved bounds through a precision change.
M1, M2 and variance are evaluated outward, with true interval squaring in the
underlying arithmetic and exact Fraction width checks against 1/10^50.
The root stops only on the required moment widths, not rounded phi zero.
No native point or trace supplies a reference interval. The 80/160/320
schedule, midpoint/retry distinction and finite entry ceilings are explicit.
Previously admitted bounds and completed reconstruction members survive later
refusal. Final encoding refusal returns the entire in-memory receipt with
its separate failure; it does not assert durable publication.

Four defects in a04 were independently or author identified and retained:

1. The author's owed midpoint retry was erased by an intervening initial-sign
   retry. The pending flag now remains set until that midpoint is evaluated.
2. A context-factory refusal during retry inherited the unregistered stage
   precision-retry. Parameter preparation is now entered/counted before the
   factory, with row Precision null until actual context admission.
3. Oversized raw input reached hashing before its 64-KiB cap. The finite
   65,537-byte trap now refuses before hash or solver entry.
4. Exhausted midpoint budget inherited reconstruction as its failure stage.
   The guard now names midpoint without inventing an attempted row or count.

The retained failing regressions show the actual earlier defects: zero
midpoint evaluations instead of one, wrong context stage, an oversized-input
hash entry reported as Unexpected, and reconstruction instead of midpoint.
The first author's regression ran a developing combined module, not the
entire a04 file. An independent AST comparison confirms that its `_Root`,
`_take`, `_input`, `_caller` and `reference_root` definitions equal a04 exactly;
the distinct whole-module fingerprints remain visible. Later regression
snapshots represent their actual successive correction states, not rewritten
original code.

All 29 original root custody records and all 90 combined records match their
stored, single-member raw and original-file identities. Root custody totals
134,879 original / 39,293 stored bytes; combined custody totals 802,922 /
192,072 bytes. Every retained attempt's three source snapshots match its
source table. The root's first run had 41 passes and one test-fixture encoder
failure before the intended API entry, followed by 45 passes. The combined
runs retain 81, 82, 85, 86 and 88 unit passes with their distinct typing,
style and formatter history. These are unit controls, not the frozen 88
experimental calls. No complete numerical roster was run by this review.

Two certificate findings remain open at exact 51a96:

1. A normally returned bare reference API Failure is replaced by
   NoRootEnclosure before its actual five fields are retained. The proposed
   schema-compatible correction preserves that original Failure in certificate
   Outcome.Failure, with Reference null and ReferenceRootCalls one. A complete
   returned root receipt with numeric refusal remains a different case: retain
   that receipt in Reference before reporting NoRootEnclosure.
2. Native trace admission checks only present rows. A complete candidate can
   therefore omit its whole trace and supply zero phi/midpoint/log/exp counts
   while retaining Starts and ObjectiveEntries one. Required stage chronology,
   used/null fields, cardinalities and counter associations must be admitted
   before independent numeric work. This is finite structural validation,
   not binary64 arithmetic replay or evidence of physical invocation.

The owner accepted both findings and is preserving discriminating regressions
before a separate correction pin. This review does not infer that correction's
success from a proposal. The post-root coordinate and original-objective
formulas, fixed context, exact tolerance comparisons and existing partial
observations were read, but whole certificate acceptance waits for that pin.
Final process/source custody, actual certified baseline and 40-case/88-call
completion remain separate coordinator obligations.

~~~text
Agency-Signature-Version: 1
Agent: Vera
Agent-Runtime: OpenAI Codex
Agent-Model: GPT-6 Astra
Credential-Identity: AceHack
Credential-Mode: shared
Human-Review: not-implied-by-credential
Human-Review-Evidence: none
Action-Mode: autonomous-fail-open
Task: 081M1Z63YMC087G0R003N5FH9X
Co-Authored-By: Codex <noreply@openai.com>
~~~
