# Guarded hidden-switch compilation: coordinator primitives

Date: 2026-09-07
Operational status: research-grade
Lifecycle: active
Work item: 081M1XXWTTF087G0R000X1HMD0
Author: Vera, OpenAI Codex using GPT-6 Astra
Artifact status: exact comparison and structural validation; actual coordinator pending

The shared [cost admission](../../src/Interp.Python/zeta_interp/hidden_switch_compiled_cost_ledgers.py)
now uses the same exact descriptive-ratio and required-half-median boundaries
as the planned conformance fixtures. Reduced ratios retain decimal numerator
and denominator strings. Zero native CPU or allocation yields null with its
named reason; zero measured native wall refuses. A zero compiled numerator
over a positive native denominator remains the exact ratio 0/1. Required
medians use integer comparisons, including values above binary64's exact
integer range. Tests inject actual helper refusals to check that production
cost admission calls both shared boundaries.

At `1fece22bc84153dae0128c3a90bbca2ed09772dc`, all 89 cost-ledger tests
pass in 4.01 seconds; strict source/test mypy, Ruff and format checks pass.
The [independent resource review](2026-09-07-hidden-switch-compiled-resource-helper-review.md)
records the source disposition separately from execution evidence.

The [coordinator contract](../../src/Interp.Python/zeta_interp/hidden_switch_compiled_conformance.py)
fixes the reviewed 92-case, 136-operation order, controls and input roles.
Its prefix validator retains every structurally checked row and call, including
an incomplete last case. Its public-result encoder preserves actual dataclass
types and every field, exact bytes, integer values and signed floating zero.
It refuses cycles, unsupported objects and declared structural bounds. Those
bounds are not an aggregate output quota. It does not instantiate a type named
by producer JSON or serialize the private certificate capability.

At `2e0d01b296d16ef8a67d78d50bcd4501900b089b`, all 41 structural tests
pass in 3.80 seconds; strict source/test mypy, Ruff and format checks pass.
The first mypy pass reported two object-iteration typing errors. The correction
uses explicit casts only after exact tuple/list checks; both outcomes remain
preserved. The [independent structural review](2026-09-07-hidden-switch-compiled-conformance-structure-review.md)
accepts that source with actual replay and aggregate retention still pending.

The combined compiled Python suite at that same source commit passes
**802 tests in 61.53 seconds**. This is not the full interpreter/native gate
and does not count as execution of the actual 92-case coordinator. The
[lossless inventory](hidden-switch-compiled-validation/2026-09-07/coordinator-primitives/manifest.json)
retains the complete first and corrected local outputs, final source identities
and combined-suite log. No registered source stream was generated by these
checks. Complete source, runtime, scientific phase and outer-envelope admission
remain pending.
