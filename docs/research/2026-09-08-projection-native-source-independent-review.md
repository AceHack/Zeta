# Independent native projection source and custody review

Date: 2026-09-08 UTC
Operational status: research-grade
Status: bounded source and preparation acceptance
Reviewer: Vera, OpenAI Codex using GPT-6 Astra
Work item: 081M1Z63YMC087G0R003N5FH9X

I accept native source bf2da44b94e770d21add73ce53dacb8fb34259bf and the
preparation inventory at cb0e899b0527d85b0d86f4e99288b64bb854b109 within the
frozen scalar-candidate scope. This read-only review covers the numeric
module, Replay script, dedicated tests, both project links and implementation
report. The [independent custody audit](projection-native-source-review/2026-09-08/README.md)
reads Git blobs and retained records only. It executes no project code,
solver, numerical fixture, native command or final comparison.

The numeric module is 36,582 bytes, SHA256
288e0348edc4babeadb993ba1b80c1a45b6d426bab80e14c5e066165d5730d0d.
Replay is 12,723 bytes, SHA256
c1174406cc4e7d3164714c536207ad2bd72f085a38a3a816c8bc1b14ade917b2.
Tests are 18,076 bytes, SHA256
b1f2aa954c1186da8471b4cdf3e8a6fadcdc2ffc0a4c1fddff1db03027c66df5.
The unchanged original objective kernel has SHA256
4004196cb0ade8527dfebf83fbb3e6e42bd36208e38affd09906787a84901c02.
All fourteen preparation source pins match their exact source-commit blobs.

The [contract review](2026-09-08-projection-contract-independent-review.md)
remains the mathematical authority for this scope. The source follows the
frozen order for log(t), log(c), A, B, D, initial log-coordinate bracket,
phi and the midpoint. It evaluates both endpoints before rounded-sign
admission. Each bounded midpoint retains its attempted point, actual phi
entry, update and selected x/q pair. Failure before phi is not counted as a
phi call; exhaustion after an update retains that update. Rounded-zero and
rounded-width stopping remain diagnostics requiring an independent certificate.

Reconstruction computes R=t*q, v=(1/t)/(1+q) and
m=((log(t)+x)-log(c))-v/2 in the declared order. Finite/positive checks and
nonzero multiplication/division underflow refusals preserve admitted partials.
It then calls the existing original objective once at the actual candidate;
its complete value and both derivatives, or original typed kernel error,
are retained. No stationarity identity replaces that operation. Numeric
Starts, log/exp, phi, midpoint/update and objective counters are incremented
at their corresponding entries, including failed entries. Solver log/exp
counters exclude the original objective's internal operations.

Successful native traces follow input, parameters, left endpoint, right
endpoint, one or more midpoints, reconstruction and objective. A typed or
caught unexpected stage failure terminates the actual prefix. The finite
source limits imply at most 262 rows; the one-attempt profile does not
silently increase its allowance. These are source/counter observations,
not an independent arithmetic replay or physical invocation proof. The
separate Python certificate review found missing structural trace admission;
that finding is being repaired in that consumer, not attributed to this producer.

Input admission caps bytes before hashing/parsing, rejects duplicate decoded
keys and invalid UTF-8, and checks the exact fixed nested schema. Decimal
length/grammar and exponent magnitude precede integer construction. Exact
rational arithmetic retains the requested value, rendered dyadic and drift,
including signed zero. The two registered clarifications are required in
the caller binding map. T/C rendered zero preserves the admitted target and
refuses at parameter-domain entry; U/K rendered zero remains admissible.
Caller metadata and its supplied source hashes are not treated as proof of
loaded code. The coordinator must bind the actual complete source roster.

Replay creates the output exclusively, admits bounded input/binding bytes,
and retains each successful file observation before the next dependent read.
Its Unix leaf open is nonblocking and no-follow; subsequent admission is a
seekable handle, fixed initial size, exact bytes plus one and unchanged size.
The declared premise remains stable owned files, not hostile namespace or
atomic immutability. The ten-second check is between reads, not kernel
cancellation. Bayesian/Core file observations are distinct from complete
loaded-dependency custody. The external coordinator still owns process,
source, binary and input/output preservation.

A numerical refusal can have a complete, exit-zero publication. Encoding,
write, flush or close failure instead leaves the full actual native receipt
in the returned RunResult, preserves the first command failure and retains
secondary close errors separately. The stdout side report carries availability,
kind and counts; it does not substitute for a missing complete receipt.
Memory retention does not imply durable recovery after storage or process
failure. The 2-MiB receipt and 128-KiB newline-inclusive side-report ceilings
are emitted-byte limits, not peak-allocation claims. The script exits two
for command/publication refusal and performs no automatic numerical retry.

All 86 complete preparation records match stored, single-member decompressed
and original-file identities: 537,501 original bytes and 135,186 stored bytes.
The explicitly incomplete first preservation manifest retains 84 gzip records
and its source-pin failure for nonexistent Directory.Build.targets. That
failure is labelled a transcribed tool observation, not invented raw stderr.
The complete later preservation did not replace the earlier records or rerun
numeric work.

Actual TRX rows confirm 29 earlier and 34 final focused passes, with no failed
or skipped rows. The initial six-error native build, nine-error test compile,
Replay-link compilation failure and FSI startup failure remain retained.
The final numeric module and tests equal the final focused source snapshots.
Only the INTERACTIVE-only System.Console and System.Environment qualifications
changed afterward in Replay; the corrected no-argument startup snapshot equals
final source and returned exit two, 428 stdout bytes, empty stderr, no input,
assembly or numeric observations. The audit's first draft checked only the
Console qualification and failed its overly narrow equality assertion. Its
exact source and tool-observed failure are preserved separately, followed by
the explicit two-edit equality check. This was an audit assumption defect.

No material producer source or custody discrepancy remains in this bounded
cut. The owner's subsequently reported full gate is outside cb0e899 custody
and is not certified by this note. The final 40-case/88-call comparison,
core viability, certificate controls and process/source authentication remain
separate obligations; no graph application or training claim follows.

~~~text
Agency-Signature-Version: 1
Agent: Vera
Agent-Runtime: OpenAI Codex
Agent-Model: GPT-6 Astra
Credential-Identity: AceHack
Credential-Mode: shared
Human-Review: not-implied-by-credential
Human-Review-Evidence: none
Action-Mode: autonomous-fail-open
Task: 081M1Z63YMC087G0R003N5FH9X
Co-Authored-By: Codex <noreply@openai.com>
~~~
