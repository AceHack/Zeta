# Projection driver: independent source and custody review

Date: 2026-09-08 UTC
Operational status: research-grade independent source review
Reviewer: Vera, OpenAI Codex using GPT-6 Astra
Work item: 081M1Z63YMC087G0R003N5FH9X

Source acceptance binds driver `1f4db4b7ad2953648a37a38d0613bb99fc7d0633`:
40992 bytes, SHA256
`83537F7105042FC077D348EC6F59FD8D44318C0233D1804B01381A6A4EE51706`.
I read the complete driver, dedicated tests and report. No outstanding material
source finding remains within this component's stated boundary. Final tests and
follow-up evidence are accepted at `53cf6cdaa75f9dcb1ab1de2c48144685fdc286d5`;
the production driver bytes are identical. No final
40-case/88-call workload, solver or native process was executed by this review.

## Admission, dispatch and shared retention

The 64-KiB raw manifest is admitted against an independently supplied uppercase
digest before parsing. The exact 47 ordered source paths, integer/hash types,
8-MiB individual/64-MiB aggregate source limits, protocol and five native roles
are source-selected. Script source and native identities must agree. The three
referenced copied DLLs remain distinct from the two assembly observations in
actual producer receipts; no third producer observation is invented.

Both descriptor-relative source scans use those same independent expectations.
Each actual read is appended before hash/match admission; source metadata omits
large bodies and explicitly leaves full read returns in memory. Fourteen actual
local module file/origin paths are checked against the selected source root.
That observation does not prove loaded bytecode equivalence, complete third-party
closure or future immutability. The manifest still needs independent provenance
and preservation outside this driver; a self-supplied matching hash is not such
provenance.

The reservation implements the approved formula exactly:
`D = 2 * [C + M + 27 * (64 KiB + 64 KiB + 2 MiB) + 4 * 1 MiB]`.
C is the script/three DLL copy total; M is the manifest length. The inner Store
receives `256 MiB - D`, its own 8-MiB journal reservation and 422 artifact slots.
The remaining allowance must exceed journal plus the runner's 2-MiB combined
terminal reserve. This is logical raw/stored retention accounting with no
refund, not an OS disk quota, peak-memory bound or successful-roster guarantee.

The 90 outer slots are four copies, one manifest, 27 sets of three native files,
and exactly four bounded metadata roles: two source snapshots, preparation and
outer-final. No extra full RunResult dump or owned stdout log is introduced.
Each publication has one fixed filename, bounded encoding, exclusive write and
exact readback; actual observations remain accessible before later admission.

The prepared native dependencies and unchanged manifest bindings reach the
fixed runner. All three service routes check request bindings; the native route
also caps entries at 27. One runner invocation selects the source-fixed roster.
The driver trusts the accepted runner/services implementation for complete
case semantics; controlled fake RunResult fixtures demonstrate wiring and
retention, not numerical execution or scientific success.

The actual RunResult is observed before comparison of its outcome or outer
publication. A prior runner failure survives later publication failure. A store
opened before a setup refusal is finalized once; after entering the runner,
the driver does not retry the runner's finalization. The final envelope is an
explicit snapshot before its own write, with attempted-path qualifications and
false runtime/source-to-machine flags. Full actual objects remain memory-only
unless an independently checked linked inner artifact retains them.

The CLI accepts only its six named arguments in fixed order and retains an
attempted manifest-read observation. Its small stdout locator and exit code
remain separate from the inner scientific result. Short output writes refuse;
this is not a crash-proof or always-available output guarantee.

## Actual source validation

The [independent custody audit](projection-driver-independent-review/2026-09-08/README.md)
verified all 53 original source-validation records: 384984 raw bytes and 94792
stored bytes. Every bounded single-member gzip, raw hash and local original
matches. Final source/test copies equal the pinned Git blobs; all fourteen
ordinary-import module observations match that source cut. Six imported source
and test files match accepted runner e133 and process 8254 exactly.

Initial validation preserves 48 passing tests and one genuine hash-after-read
retention failure, plus 107 typing diagnostics and style/format findings. The
repaired source appends the actual read before hashing. Attempt 2 has 57 tests
passing and 20 typing diagnostics; attempt 3 has 57 passing in 5.02 seconds,
strict mypy, Ruff and format clean. The separate ordinary-import observation
ran package initialization but no solver or native service. The retained quick
gate reports all 16 checks passed. These records do not retain every ephemeral
unit-fixture filesystem tree or every fixture return, and are not the final
40/88 experiment.

## Withdrawn locator finding

After the original source pin, the author suspected that terminal/journal links
needed an explicit `records` base. I relayed that preliminary suspicion before
checking the Store's exact descriptor construction. The proposed test exposed
the mistaken premise: `_append` already uses `records/record-...`, and
`finalize` uses `records/final-journal.json`. Both resolve directly from
AttemptRoot. Adding another base would duplicate the prefix.

I checked both Store constructors and corrected that relay. Original driver
source was not defective. The author withdrew the unnecessary source addition. The separate 38-record
follow-up preserves both mistaken fixture versions and the transient source:
241696 raw bytes and 57113 stored bytes, all identities and originals verified.
The first two controls failed on the unchanged driver because they demanded
the unnecessary field; the next two failed with duplicated records/records
paths after the transient addition, while 57 other tests passed.

The final source is byte-identical to 1f4. Final tests resolve actual terminal,
journal and setup-finalization descriptors from AttemptRoot, then verify file
length/hash. Three existing setup controls now require no invented Run/journal
before opening the Store. Final retained source/test snapshots bind 59 passing
tests in 4.84 seconds and clean strict mypy/Ruff/format. The original 1f4 normal
push and exact remote proof also match. This is a false-positive finding history
and added test coverage, not a repaired runtime defect.

Both independent archive audits and the source-binding audit exited zero with
empty stderr. No archived script or project test was executed during review.

Signed: Vera, OpenAI Codex using GPT-6 Astra.
