# Registered scalar projection: independent outcome and custody review

Date: 2026-09-08 UTC
Reviewer: Vera, OpenAI Codex using GPT-6 Astra
Operational status: research-grade independent review
Disposition: accepted within the frozen finite local projection scope
Work item: 081M1Z63YMC087G0R003N5FH9X

## Exact reviewed cut

This review accepts the actual `registered-1` records preserved in publication
commit `61f8e14ba9f89e790dce635ce5c86db5c619b63c`. The sole CLI executed
`4937d7468446599e966adc65c0ca1e9be54472aa`, with implementation source
`f33ac42959388344fc3c82058f165764954fa22d` and independently supplied manifest
SHA256 `FE1F5BDF732FA6B08092887210552BA9CEF68D724E519C77958A282366A348E7`.
The previous [archive admission](2026-09-08-projection-implementation-archive-independent-review.md)
was committed and remotely observed before execution.

The source-fixed 40 subject IDs and 88 ordered slots, their inputs, actual
native process records, native/reference/certificate returns, mutations,
descriptor links and reservations agree. No production or experimental-record
defect was found. The coordinator's
[result disposition](https://github.com/Lucent-Financial-Group/Zeta/blob/61f8e14ba9f89e790dce635ce5c86db5c619b63c/docs/research/2026-09-08-precision-gate-projection-registered-results.md)
correctly leaves cancellation uncertified and distinguishes expected refusals
from successful optimization.

The reviewer ran only new read-only standard-library/Git audits of existing
files. No project module was imported, numerical service or native process
called, exponential/logarithm evaluated, or registered workload rerun.
Exact rational arithmetic checked relations among retained values. The
[audit index](projection-registered-independent-review/2026-09-08/README.md)
preserves the executed scripts, complete outputs, one corrected reviewer
assumption and their byte identities.

## Actual outcomes

| Scope | Independently checked outcome |
| --- | --- |
| Twelve core subjects, 36 slots | Twelve native candidates, twelve reference enclosures, twelve certificates |
| Four stress subjects, 12 slots | Every actual outcome retained; cancellation candidate refused certification |
| Native and reference one-midpoint controls, four slots | Both designated limited solvers returned midpoint IterationLimit |
| Four nonpositive T/C subjects, 12 slots | Native/reference Domain; certificate no-candidate preserves native refusal |
| Six malformed-wire subjects, 12 slots | Both implementations returned Wire/input with all numeric counters zero |
| Twelve mutations, 12 slots | All refused; each derived from the actual certified core/unconstructed baseline |

The full operation counts are **13 candidates, 15 enclosures, 12 certificates,
40 refusals and eight no-candidate returns**. All 88 calls entered, returned,
retained their receipt and were checked. Raised calls, pending slots, primary
failure and secondary failures are absent in this actual run.

Native exponential-range stress returned NumericalUnderflow, with an available
reference enclosure. Parameter-range stress returned native NumericalRange
and a reference enclosure. Reference exponential-range stress returned
NumericalUnderflow from both implementations. Cancellation returned a native
candidate, reference IterationLimit and certificate NoRootEnclosure. These
stress observations carry no implied success outside the core requirements.

The audit rebuilt every ordered input from the source's fixed literal tables,
including the intentional duplicate JSON key. It checked each receipt's input
hash, numeric case ID, exact binding map and retained native bytes. The baseline
native record is `records/record-000058.bin`, 16,424 bytes, SHA256
`3D7FC8516C4428301E26A2AB82FEFB2DDEAE0853AC422BAC36AA19AB1617DC6B`.
Its actual certificate is slot 23; the first mutation is slot 76. All twelve
mutations match their exact single-field transformations of that same baseline,
without modifying a target or inventing a replacement candidate.

The mutant dispositions are NotCloseToMinimum for mean; CandidateShape for
zero variance, altered log ratio, missing objective and reversed bracket;
InconsistentCoordinate for ratio/R; ObjectiveMismatch for objective and both
gradients; TargetMismatch for target zero sign; and SourceMismatch for binding.
The changed log ratio lies outside its retained diagnostic bracket, so its
structural refusal is the actual observed boundary.

## Certificate and chronology checks

All 84 core leaf checks pass the registered exact rational tolerance test.
Across core and mutation receipts, all 110 retained leaf decisions were checked:
104 true and six false. Each native bit pattern decodes to the compared exact
dyadic, each recorded tolerance equals its fixed formula, and the maximum
distance to both retained interval endpoints agrees with the recorded result.
The six false leaves are the mean, ratio, R, objective and two gradient controls;
the other six mutations refuse before a failing leaf is produced.

Leaf intervals refer to the complete actual nested root, coordinate and
objective observations. The audit checked all 47 retained root occurrences:
28 direct calls and 19 roots returned inside certificate calls. Sequence and
entry counters, available context order, refusal terminals, positive variance,
exact `1/10^50` mean/variance width admission and final moment intersections
agree with their retained records. Every available nested root equals the full
corresponding direct result. This equality is an observation of the returned
values, not a claim that the nested call was skipped.

All certificates retain their last successful root context, coordinate/objective
returns before leaf comparison, and bounded post-root entry counts. Seven
analytically constructed centers also contain the independently calculated
exact stationary mean/variance pairs in their returned enclosures. This check
uses rational arithmetic only. The audit does not independently re-evaluate
transcendental interval endpoints or replay the native optimization trajectory;
those arithmetic guarantees rely on the separately reviewed, pinned source.

## Process, source and storage custody

All 27 native calls have unique positive PIDs, ordered start/launch/finish
observations, actual exit zero, cleanup wait exit zero, closed direct children
and readers, both stream EOF flags, empty stderr and no stream overflow or
cleanup failure. Each raw stdout decodes exactly to its retained producer
record. Producer counters, trace counts and receipt kinds match the separately
stored native receipt. The 81 input/binding/receipt paths exist and match both
launcher and producer identities. Each receipt also equals its numbered store
record byte-for-byte.

The five declared native roles match their original, pre-call and post-call
identities. The four owned copies match the pinned script and three direct DLLs.
The producer reports two actual assembly observations, matching the copied
Bayesian and Core files; the audit does not invent a third producer observation.
All 47 sources equal the frozen manifest, source Git cut, executed Git cut and
current files. Both source snapshot byte strings are identical, and their 14
observed module file/origin paths belong to the declared source roster.

The complete archive has **336 regular members**, 21,658,589 payload bytes,
and 6,397,793 stored bytes with SHA256
`04C079901CEA886500CFD0A5912CD812A997268803FAE3F0DAD4F8EDE65A8167`.
Complete single-member gzip framing, exact member inventory, all member hashes
and every original local byte string passed. All 328 actual attempt files are
present, totaling 21,527,800 file-content bytes. All **242 distinct descriptor
paths** resolve correctly; repeated descriptors agree exactly.

The journal admits 237 numbered records totaling 10,188,449 bytes. Including
its reserved four MiB per raw/stored journal copy gives the recorded 14,382,753
bytes on each side and 238 reserved slots. The outer reservation recomputes to
149,737,504 combined bytes; inner capacity is 118,697,952, totaling exactly
256 MiB. The 90 outer and 422 inner slots total 512. No reservation is refunded.
The terminal and final journal hashes match their outer descriptors, and the
final journal correctly records its snapshot before its own final write.

The external coordinator records the exact sole CLI argv, selected Python
source root, two successful prelaunch remote observations, clean executed head,
52 source/native file observations, exit zero and 40.66178766702069 elapsed
seconds. Its provenance and the later archive remain outside the explicitly
scoped attempt-retention reservation. That elapsed observation is not a speed
comparison or steady-state inference benchmark.

## Bounded next step and separate publication receipt

This result supports investing in the next fixed mixed-message schedule and
module-epoch contract. It establishes finite local Gaussian-family projection
admission for these exact native dyadic inputs. It does not make the Gaussian
the exact posterior or prove graph convergence, global learning performance,
runtime closure, source-to-machine correspondence or SOTA. Native trajectory
and graph-application flags remain false. Compiled-controller work remains
paused and streams 9307/9409 remain unopened.

The next finite integration must retain distinct evidence/cavity membership,
own-message exclusion, proper-belief checks, proposed versus applied updates,
retraction and version/weight epochs. Refusal must preserve the committed belief.
The current result does not authorize a broader training or measurement run.

Separately, the small PR 17050 custody addition was inspected in the publication
working tree by exact archive identity, before its later preservation commit.
All 55 originals match the 8,623-byte archive SHA256
`E87FBCAA7E6EAB47BD577747A4D6385A2711D0252DA9EAFCD5044F6D4902A858`.
Its complete dated check observation is 88 success/three skipped, with no
unresolved review thread. The retained normal merge command exited zero.
Existing Git objects independently confirm head `2d6161be620d6af3897a7651e11477753a4b7319`,
squash `0d29df2db91b001bb1b7e39f6c55e906116f328b`, exact whole tree
`ce3144e4055510aafecec30ff5d7dc95ff787e19` and all 17 paths counted without
rename folding. This is a dated main receipt, not a present-state repository lock.

Signed: Vera, OpenAI Codex using GPT-6 Astra.
