# Guarded hidden-switch compilation: raw certificate negative corpus

Date: 2026-09-07
Operational status: research-grade
Lifecycle: active
Work item: 081M1XXWTTF087G0R000X1HMD0
Author: Vera, OpenAI Codex using GPT-6 Astra
Artifact status: prearchive Python fixture; native conformance and runtime admission pending

Source `60eeb0b868b973298cb1c6f48044ee7172ada369` adds
[a fixed raw certificate corpus](../../src/Interp.Python/zeta_interp/hidden_switch_compiled_certificate_cases.py)
and tests under the unchanged
[registration](2026-09-07-hidden-switch-compiled-protocol.md).
The source accepts the caller's complete admitted binding map. It constructs
one valid control and thirty ordered, distinct raw input mutations, preserving
each input's bytes/hash and actual independent Python admission outcome.

The cases change protocol/source bindings, epsilon, drift, effect, depth,
expression order, complete alpha candidates, envelope/gap intervals, endpoint
margins and the final subtraction-error allowance. They swap the depth-two
guards and move each depth-two/depth-three endpoint outward by one binary64
step. Other cases alter the strictness declaration, rational spelling, integer
type and exact object shape; byte-level cases contain a duplicate key,
nonfinite number, invalid UTF-8, unpaired surrogate, empty input and truncation.
An unchanged mutant or duplicate raw input refuses corpus construction.

The focused suite passed **39 tests in 6.03 seconds**, plus strict mypy and
Ruff checks for both files. It executes the real strict JSON and independent
numeric verifier, checking one success and thirty concrete refusals. A no-op
mutation witness requires corpus construction to fail rather than report
coverage. Binding order does not change the raw corpus; a changed binding
changes the baseline identity. No study source generator or native policy runs.

The [retained fixture](hidden-switch-compiled-validation/2026-09-07/certificate-cases/cases.json)
uses the explicitly labeled placeholder `hand/source.py` binding. It is
not an assertion that such a source file was admitted. Its baseline SHA256 is
`CF3C800035F4E303FC4479F0DAC120517621093D3A1300925ACA209649900962`.
The [34-artifact inventory](hidden-switch-compiled-validation/2026-09-07/certificate-cases/manifest.json)
binds all 31 lossless raw inputs, the fixture record, the exact emitter script
and test log. Stored and original byte counts/hashes were checked separately.
The emitter records the intended implementation source pin; it does not claim
complete executing-module or runtime admission. The final full-source binding
map requires its own corpus before implementation archival and must not replace
this earlier fixture. Native outcomes are explicitly `not-executed` here.

Independent read-only review accepted this source without a material finding.
The reviewer executed no tests, native process or guard calculation. It retained
an important scope distinction: `inclusive-switch-relation` and
`reassociated-expression` mutate certificate declarations, not executing selector
or arithmetic instructions. These cases support certificate-input admission
negatives. Actual branch/graph mutation obligations require separate admitted
source and conformance evidence; a refused declaration cannot substitute for it.
