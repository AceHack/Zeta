# PR17051 independent publication review custody

Date: 2026-09-08 UTC
Author: Vera, OpenAI Codex using GPT-6 Astra
Operational status: research-grade independent review custody

The [signed review](../../2026-09-08-pr17051-publication-review.md) records the
accepted publication cut and its narrow wording correction. The
[identity manifest](audit-identities.json) binds this review's exact helpers,
lossless gzip records and reviewed document snapshots.

`audit-1` preserves the reviewer's original executed helper and filename-locator
failure. `audit-2` preserves the corrected helper, actual command/completion,
complete 1,210,333-byte stdout and empty stderr. Its executed source remains in
`audit-2-audit.py.gz`. `continuity-1` preserves the separate successful
byte-comparison command, output and original source; it imports no numerical module.

The audit checks all 162 archive members against the original files and
recomputes the expected Git tree and all 1,063 no-renames path identities. Its
complete output retains every checked member/path and the actual Git command
observations. The historical main and shared-view observations remain dated
records; no shared checkout refresh is performed by this reviewer.

The exact original/corrected integration register, packet README and manifest,
handoff, historical type-check invocation and source-cut configuration are
retained separately. `inspection-disposition.json` distinguishes earlier
tool-observed locator mistakes from the separately captured audit failure.
No generated stdout or transcript is relabeled as an original producer stream.

The helpers are fixed local review scripts using normal Python assertions.
Their scope is this known packet, not a general public archive API or an
assertions-disabled guarantee. No numerical workload or accepted proposal
review is repeated.

## Observation-call correction

The [signed follow-up](helper-correction.md) records the coordinator's finding
that subprocess-backed reads and Git observations were inside assertions.
The current helpers assign those call results before asserting their values.
They are bound to fresh `audit-3` and `continuity-2` executions. Historical
executions remain bound to their original gzip source preimages.

The original identity manifest and README also have exact gzip preimages.
[helper-correction.json](helper-correction.json) maps every historical current
file identity to its original bytes, verifies all 22 earlier gzip records
unchanged, and records the fresh result comparison. The main audit's actual
Git timestamps differ; its remaining result and command-observation fields
are identical. Continuity stdout is byte-identical. Both fresh runs returned
zero with empty stderr. No assertions-disabled run or safety claim is added.
