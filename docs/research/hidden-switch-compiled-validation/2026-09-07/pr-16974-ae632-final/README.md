# Historical capture five: reviewed publication on main

Date: 2026-09-07
Operational status: research-grade
Lifecycle: landed
Work item: 081M1YPKNM3087G0R001M4RW87
Author: Vera, OpenAI Codex using GPT-6 Astra
Artifact status: publication proof; parent implementation and runtime admission pending

[PR #16974](https://github.com/Lucent-Financial-Group/Zeta/pull/16974)
merged at 2026-09-07T21:06:39Z with a head-guarded squash. Reviewed head
`ae632b7396e7195097ed92e8b5832b42f9cb9489` became main commit
`0c42b307f74e5298ca90fd1b769621aa1d9a80c6`. The actual merge message
contains the complete publication task's AgencySignature. All 2,566 published
tree entries match exactly between the reviewed feature, merge and observed
main, including absent deleted paths. All 26 API file pages were collected
between matching head observations; no truncated first-page inventory was used.

Local full preflight passed all 18 checks at its recorded publication base.
Current PR CI separately passed the required gate, both TypeScript test jobs
and all three current native build/test jobs: macOS 26, Ubuntu 24.04 and Ubuntu
24.04 ARM. The complete bounded observer found no unresolved review thread.
The exact reviewed PR body and live required-check rule were checked before
merge. GitHub's PR, Actions, API and Webhooks components were operational.

The advisory `drift (loud)` job remained red. Its retained historical window
reports 53 failures out of 59 Windows ARM and Windows 2025 executions, plus
3 failures out of 60 macOS executions. These are historical observations,
separate from the current PR's green native jobs. The live main ruleset names
only `gate (required)`, which passed; the workflow explicitly excludes the
historical drift reporter from that dependency. The red was inspected and
preserved, not relabeled green or bypassed with an administrative merge.

The [lossless manifest](manifest.json) retains 35 records: complete pagination,
successive gate observations, exact rules/body, historical drift log, dependency
state, merge result, proof script and final tree/ancestry proof. The first
observer attempt could not use the local Zeta token store. A second observer
used the already authenticated `gh api` transport while keeping the same
paginated adapter. That preparation failure is retained; no token was exposed
or new authentication requested.

This publication preserves the original historical graph-capture bytes and
their independent review. It does not admit method extents, complete closure,
runtime identity or the compiled scientific experiment. Parent work item
081M1XXWTTF087G0R000X1HMD0 remains active, with registered streams unopened.
