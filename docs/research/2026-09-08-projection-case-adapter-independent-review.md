# Independent projection case-adapter review

Date: 2026-09-08 UTC
Operational status: research-grade
Status: bounded source and custody acceptance
Reviewer: Vera, OpenAI Codex using GPT-6 Astra
Work item: 081M1Z63YMC087G0R003N5FH9X

I accept the case adapter and dedicated tests at
2ba65308e6e0a520634c8a8bac49aa4f0c925b3a within their declared rendering
and transformation scope. The [independent custody audit](projection-case-adapter-review/2026-09-08/README.md)
uses source AST literals and retained records; it executes no adapter,
solver, reference, certificate, native process or final comparison.

The adapter source is 9,695 bytes, SHA256
d5ea9704c1e33429d990f66ba862bc5ed25a1f5c7c9a15e33a8b8d621ca71124.
Its tests are 6,185 bytes, SHA256
7d60a095da1c5e536e657900d32c7c50a08bc8ee44928af9a1268c2f4d7b7f61.
The exact three shared admission, software-IEEE and bounded-encoding
dependencies are included in the passing attempt's five-file identity roster.

All 21 base target rows match the frozen contract literals and order,
including the archived Cstar string, signed-zero U and subnormal-producing
decimal input. Native-one and reference-one profiles apply only to their
named controls. The fixed construction yields 40 distinct outer IDs and
88 planned top-level slots: 63 base, one reference-limit, 12 wire and
12 certificate calls. This is a static plan, not evidence of execution.
The [contract review](2026-09-08-projection-contract-independent-review.md)
retains the independent mathematical and count derivation.

Input rendering selects only a fixed registered subject, retains parameter
strings and uses the 64-KiB bounded encoder. Each wire control starts from
core/unconstructed; its input Id remains that subject. Boolean, nonfinite
spelling, missing member and extra-member changes match the named controls.
The duplicate-T case first checks one exact encoded insertion site, inserts
the second key once, then rechecks the byte bound. It does not normalise the
duplicate away before the real wire boundary can observe it.

Certificate transformations strictly decode at most 2 MiB into a fresh
tree and modify the named field once. They retain the numeric subject and
original input/binding fields except the expressly named mutation. The
1024 and positive-zero encodings are exact. The six +1 changes decode finite
bits to an exact Fraction, add one and round once with the independent
nearest-even implementation. The halfway test correctly preserves a case
where the +1 is absorbed; the helper does not invent a different mutation
to force a refusal. The real baseline and later certificate result must
therefore establish discrimination. Positive-to-negative target-zero
mutation checks the original sign; reversed bracket, missing objective and
zeroed protocol binding match the fixed roster.

The helper does not authenticate a complete candidate or prove that it was
certified. A minimally usable transformation input can be a disposable unit
fixture; that fact is explicitly labelled. Before registered certificate
controls, the coordinator must retain the actual certified core/unconstructed
baseline and its independently expected bindings, then use the immutable
original bytes for every control. Missing or uncertified baseline leaves
those calls incomplete. Full receipt admission and expected refusal remain
with CertifyNative and the complete recorder/comparator, not this helper.

All 14 retained gzip records match their stored, complete single-member raw
and original-file identities: 2,363 original bytes and 1,287 stored bytes.
All five passing-attempt source identities match the exact commit. The
initial import-order diagnostic and global-interpreter pytest launch failure
remain separate from the project-environment run of 31 passing tests, Ruff
and strict typing. The initial preformat source was not separately captured;
the report acknowledges that limitation rather than backfilling its identity.
No dependency installation or scientific run is inferred from this repair.

No material source or custody discrepancy was found. This acceptance does
not establish core viability, any expected native refusal, a successful
certificate, actual call counts or final 40-case/88-call completion.

~~~text
Agency-Signature-Version: 1
Agent: Vera
Agent-Runtime: OpenAI Codex
Agent-Model: GPT-6 Astra
Credential-Identity: AceHack
Credential-Mode: shared
Human-Review: not-implied-by-credential
Human-Review-Evidence: none
Action-Mode: autonomous-fail-open
Task: 081M1Z63YMC087G0R003N5FH9X
Co-Authored-By: Codex <noreply@openai.com>
~~~
