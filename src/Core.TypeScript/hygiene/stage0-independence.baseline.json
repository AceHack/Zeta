{
  "independent": 18,
  "bytes": 453975,
  "exceptions": [
    "full-ai-cluster/nixos/modules/k3s-datastore-preflight.sh \u2014 systemd ExecStartPre for k3s.service, not a shell-spawned child. The refusal it implements (a surviving server datastore on a box being re-flashed as a worker) must run BEFORE k3s starts, in a unit ordered ahead of it, so no stage-0 shell script can be its parent: `k3s-datastore-preflight.nix` sets ExecStart = bash ${preflightScript}. Routing it behind an existing door would mean running it after the thing it exists to gate. Kept a standalone .sh rather than inlined into the Nix string so its logic is EXECUTABLE against fixture disks in a unit test (see the module docstring) \u2014 a door admitted so the falsifier can reach it.",
    "full-ai-cluster/nixos/modules/k3s-join-intent-preflight.sh \u2014 the sibling of k3s-datastore-preflight.sh above, admitted for the same structural reason and covering the case that one explicitly does not. It is a systemd oneshot ordered `before` k3s.service and `requiredBy` it, so no stage-0 shell script can be its parent: routing it behind an existing door would mean running it after the thing it exists to gate. It compares the join intent on disk (/etc/zeta/cluster-join-server-url) against what Nix evaluation actually RESOLVED, which is the only way to observe the pure-eval reversion `injected-server-join.nix` names in its own header \u2014 a joining server silently reverting to a FOUNDING one. The datastore preflight cannot catch it, by its own admission: \"On a genuinely from-scratch flash that is fine \u2014 there is no datastore, so nothing is ignored\", and a from-scratch flash is exactly how a second machine is added. Kept a standalone .sh rather than inlined into the Nix string so its logic is EXECUTABLE against fixtures in src/Core.TypeScript/hygiene/lint-k3s-join-intent-preflight.test.ts \u2014 a door admitted so the falsifier can reach it.",
    "full-ai-cluster/nixos/modules/k3s-agent-tls-self-heal.sh (WP25, 081M38G8NGC087G0R001GEGEDK) \u2014 the third sibling of the two above, admitted for the identical structural reason: it is a systemd ExecStartPre on systemd.services.k3s (both k3s-server.nix and k3s-agent.nix), ordered BEFORE k3s starts, so no stage-0 shell script can be its parent \u2014 routing it behind an existing door would mean running it after the thing it exists to gate. It removes zero-length files under /var/lib/rancher/k3s/agent, the self-heal for a truncated write from an unclean stop (measured on run 35927439681: every agent cert/kubeconfig file present but 0 bytes, k3s retrying \"error loading key ...: <nil>\" forever \u2014 see the module's own header for the rancher/dynamiclistener citation). Kept a standalone .sh rather than inlined into the Nix string so its logic is EXECUTABLE against fixture directories in src/Core.TypeScript/hygiene/k3s-agent-tls-self-heal.test.ts \u2014 a door admitted so the falsifier can reach it.",
    "full-ai-cluster/nixos/modules/k3s-datastore-bootstrap-recovery.sh (081M39CR74D087G0R002BEG2G4) \u2014 a systemd service ordered AFTER k3s.service (not before, unlike its two siblings above -- it can only decide anything once k3s has actually tried and failed), polling systemctl/journalctl to recover a STILLBORN datastore (one that never completed bootstrap, e.g. a power cut in the first ~20s of first boot) while never touching one that has genuinely served. No stage-0 shell script can be its parent: it must run on the boot path with no bun available, and its decision logic must speak on metal with no harness present (081M39B2MDA087G0R003CCEJPQ measured that a WP11-marker-gated diagnostic can go silently unrun). Kept a standalone .sh rather than inlined into the Nix string so its logic is EXECUTABLE against fixtures in src/Core.TypeScript/hygiene/k3s-datastore-bootstrap-recovery.test.ts, whose has-served suite is the falsifier for the one property this pair exists to guarantee \u2014 a door admitted so the falsifier can reach it.",
    "full-ai-cluster/nixos/modules/k3s-datastore-bootstrap-sentinel-write.sh \u2014 the write side of the same guard as its sibling immediately above, admitted for the identical structural reason: it must observe k3s's real readyz endpoint on the boot path with no bun available, and its write-once behaviour is what the has-served falsifier in k3s-datastore-bootstrap-recovery.test.ts depends on being EXECUTABLE, not merely read \u2014 a door admitted so the falsifier can reach it."
  ]
}
